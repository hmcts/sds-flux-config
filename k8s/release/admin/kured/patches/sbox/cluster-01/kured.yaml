---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: kured
  namespace: admin
spec:
  values:
    extraArgs:
      slack-username: sbox-01-aks
      slack-channel: aks-monitor-ss
    tolerations:
    - key: "CriticalAddonsOnly"
      operator: "Equal"
      value: "true"
      effect: "NoSchedule"