apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  annotations:
    flux.weave.works/automated: "false"
  name: traefik2
  namespace: admin
spec:
  values:
    service:
      spec:
        loadBalancerIP: ""