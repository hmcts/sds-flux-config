apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: traefik
  namespace: admin
spec:
  values:
    loadBalancerIP: 10.140.15.250
    dashboard:
      domain: traefik00.ss-service.core-compute-sandbox.internal
    accessLogs:
      enabled: true
    autoscaling:
      minReplicas: 1
      maxReplicas: 2
    ssl:
      generateTLS: true
      defaultCN: "*.ss-service.core-compute-sandbox.internal"