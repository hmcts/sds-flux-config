---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}-svc
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - protocol: TCP
      port: {{ .Values.haPort }}
      targetPort: {{ .Values.haPort }}
  selector:
    app: {{ .Values.appName }}