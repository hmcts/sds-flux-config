apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hmi-pact-broker.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "hmi-pact-broker.name" . }}
    helm.sh/chart: {{ include "hmi-pact-broker.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "hmi-pact-broker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never