apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: traefik-no-proxy
  namespace: admin
spec:
  values:
    service:
      spec:
        loadBalancerIP: "20.90.205.154"
    ingressClass:
      enabled: true
      isDefaultClass: true
    ports:
      web:
        redirectTo: websecure