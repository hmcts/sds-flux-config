apiVersion: kustomize.config.k8s.io/v1beta1
namespace: admin
kind: Kustomization
commonLabels:
  k8s-app: aad-pod-id
resources:
# TODO when upgrading this remove patches/override-version.yaml
  - https://raw.githubusercontent.com/Azure/aad-pod-identity/v1.8.4-rc.0/deploy/infra/deployment-rbac.yaml
  - mic-exception.yaml
patchesStrategicMerge:
  - patches/override-version.yaml
  - patches/aad-pod-id.yaml
