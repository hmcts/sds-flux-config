---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: pip-business-rules
  namespace: pip
  annotations:
    fluxcd.io/automated: "true"
    filter.fluxcd.io/nodejs: glob:prod-*
spec:
  releaseName: pip-business-rules
  chart:
    git: git@github.com:hmcts/pip-business-rules
    path: charts/pip-business-rules
    ref: master
  values:
    java:
      disableTraefikTls: true
