# version: 1
# commandUpdated:
#   generators:
#     - command: kustomize build --load_restrictor none .
version: 1
patchUpdated:
  generators:
    - command: kustomize build --load_restrictor none .
  patchFile: flux-patch.yaml