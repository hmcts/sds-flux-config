apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "az-storage-backup.fullname" . }}-test-connection"
  labels:
{{ include "az-storage-backup.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "az-storage-backup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
