apiVersion: v1
kind: Service
metadata:
  name: {{ include "jd-proxy.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "jd-proxy.name" . }}
    helm.sh/chart: {{ include "jd-proxy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "jd-proxy.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
