apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: vh-admin-web
spec:
  interval: 10m
  releaseName: vh-admin-web
  values:
    java:
      image: sdshmctspublic.azurecr.io/vh/admin-web:staging-8017d13-202207121146 # {"$imagepolicy": "flux-system:vh-admin-web"}
      disableTraefikTls: true
  chart:
    spec:
      chart: ./stable/vh-admin-web/non-prod
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 5m
  install:
    disableWait: true
  upgrade:
    disableWait: true
