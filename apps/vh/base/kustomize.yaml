apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: vh
  namespace: flux-system
spec:
  path: ./apps/vh/${ENVIRONMENT}
  postBuild:
    substitute:
      NAMESPACE: "vh"
      TEAM_NOTIFICATION_CHANNEL: "${ENV_MONITOR_CHANNEL}"
