apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: promtail
  labels:
    release: kube-prometheus-stack
spec:
  values:
    config:
      clients:
        - url: https://sds-loki-01.dev.platform.hmcts.net/loki/api/v1/push