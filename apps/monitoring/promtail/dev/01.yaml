apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: promtail
  labels:
    release: kube-prometheus-stack
spec:
  values:
    config:
      clients:
        - url: https://loki-00.ptl.platform.hmcts.net/loki/api/v1/push