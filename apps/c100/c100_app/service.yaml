apiVersion: v1
kind: Service
metadata:
  name: c100-application-service
  namespace: c100
  labels:
    app: c100-application-web
spec:
  ports:
  - port: 80
    name: http
    targetPort: 3000
  selector:
    app: c100-application-web