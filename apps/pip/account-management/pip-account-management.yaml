apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pip-account-management
spec:
  releaseName: pip-account-management
  values:
    java:
      image: hmctsprod.azurecr.io/pip/account-management:prod-37c917a-20260126093640 # {"$imagepolicy": "flux-system:pip-account-management"}
      disableTraefikTls: true
  chart:
    spec:
      chart: ./stable/pip-account-management
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 5m
  install:
    disableWait: true
  upgrade:
    disableWait: true
