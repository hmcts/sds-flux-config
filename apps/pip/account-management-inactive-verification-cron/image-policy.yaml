apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: pip-account-management-inactive-verification-cron
spec:
  imageRepositoryRef:
    name: pip-account-management-inactive-verification-cron