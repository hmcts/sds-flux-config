apiVersion: image.toolkit.fluxcd.io/v1alpha2
kind: ImagePolicy
metadata:
  name: pip-account-management-inactive-verification-cron
spec:
  imageRepositoryRef:
    name: pip-account-management-inactive-verification-cron