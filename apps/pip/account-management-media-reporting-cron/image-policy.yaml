apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: pip-account-management-media-reporting-cron
spec:
  imageRepositoryRef:
    name: pip-account-management-media-reporting-cron