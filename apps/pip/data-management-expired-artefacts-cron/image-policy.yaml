apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: pip-data-management-expired-artefacts-cron
spec:
  imageRepositoryRef:
    name: pip-data-management-expired-artefacts-cron