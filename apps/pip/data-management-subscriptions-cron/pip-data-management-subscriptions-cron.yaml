apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: pip-data-management-subscriptions-cron
spec:
  releaseName: pip-data-management-subscriptions-cron
  values:
    job:
      image: sdshmctspublic.azurecr.io/pip/cron-trigger:prod-67e3e9c-20231214142801 # {"$imagepolicy": "flux-system:pip-data-management-subscriptions-cron"}
      disableTraefikTls: true
  chart:
    spec:
      chart: ./stable/pip-cron-trigger
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 5m
  install:
    disableWait: true
  upgrade:
    disableWait: true
