apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: pip-data-management-subscriptions-cron
spec:
  releaseName: pip-data-management-subscriptions-cron
  values:
    job:
      image: sdshmctspublic.azurecr.io/pip/cron-trigger:prod-dbccea1-20230531084445 # {"$imagepolicy": "flux-system:pip-data-management-subscriptions-cron"}
      disableTraefikTls: true
  chart:
    spec:
      chart: ./stable/pip-cron-trigger
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 5m
  install:
    disableWait: true
  upgrade:
    disableWait: true
