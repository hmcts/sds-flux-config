apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: pip-publication-services-mi-reporting-cron
spec:
  imageRepositoryRef:
    name: pip-publication-services-mi-reporting-cron