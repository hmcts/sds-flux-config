apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: pip-data-management-no-match-artefacts-cron
spec:
  releaseName: pip-data-management-no-match-artefacts-cron
  values:
    job:
      image: sdshmctspublic.azurecr.io/pip/cron-trigger:pr-16-f60a52f-20221011092914 # {"$imagepolicy": "flux-system:pip-data-management-no-match-artefacts-cron"}
      disableTraefikTls: true
      environment:
        DATA_MANAGEMENT_URL: https://pip-data-management.test.platform.hmcts.net
        ACCOUNT_MANAGEMENT_URL: https://pip-account-management.test.platform.hmcts.net
        TRIGGER_TYPE: NO_MATCH_ARTEFACTS
  chart:
    spec:
      chart: ./stable/pip-cron-trigger
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 5m
  install:
    disableWait: true
  upgrade:
    disableWait: true
