apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pip-data-management
spec:
  releaseName: pip-data-management
  values:
    java:
      image: hmctsprod.azurecr.io/pip/data-management:prod-f1c3ef0-20260312112256 # {"$imagepolicy": "flux-system:pip-data-management"}
      disableTraefikTls: true
  chart:
    spec:
      chart: ./stable/pip-data-management
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 5m
  install:
    disableWait: true
  upgrade:
    disableWait: true
