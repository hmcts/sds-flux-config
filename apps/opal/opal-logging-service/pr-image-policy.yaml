apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: opal-logging-service-pr
  annotations:
    hmcts.github.com/prod-automated: disabled
spec:
  imageRepositoryRef:
    name: opal-logging-service
  filterTags:
    extract: $ts
    pattern: '^prod-1917-[a-f0-9]+-(?P<ts>[0-9]+)'
  policy:
    alphabetical:
      order: asc