apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: opal-user-service-pr
  annotations:
    hmcts.github.com/prod-automated: disabled
spec:
  imageRepositoryRef:
    name: opal-user-service
  filterTags:
    extract: $ts
    pattern: '^prod-[a-f0-9]+-(?P<ts>[0-9]+)'
  policy:
    alphabetical:
      order: asc