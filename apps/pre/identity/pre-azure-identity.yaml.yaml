apiVersion: "aadpodidentity.k8s.io/v1"
kind: AzureIdentity
metadata:
  name: pre
  namespace: pre
spec:
  type: 0

---

apiVersion: "aadpodidentity.k8s.io/v1"
kind: AzureIdentityBinding
metadata:
  name: pre-binding
  namespace: pre
spec:
  azureIdentity: pre
  selector: pre
