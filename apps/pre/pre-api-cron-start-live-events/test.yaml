apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pre-api-cron-start-live-events
  namespace: pre
spec:
  values:
    global:
      jobKind: CronJob
    job:
      suspend: false
      schedule: "00 13 * * *"
      image: hmctsprod.azurecr.io/pre/api:prod-97ef79a-20260707144126 # {"$imagepolicy": "flux-system:pre-api"}
      environment:
        PLATFORM_ENV_TAG: Testing
        PORTAL_URL: https://pre-portal.test.platform.hmcts.net/
