apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: pre-api
spec:
  interval: 10m
  releaseName: pre-api
  values:
    java:
      image: sdshmctspublic.azurecr.io/pre/api:prod-495b59c-20231106100525 # {"$imagepolicy": "flux-system:pre-api"}
      disableTraefikTls: true
  chart:
    spec:
      chart: ./stable/pre-api
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 5m
  install:
    disableWait: true
  upgrade:
    disableWait: true
