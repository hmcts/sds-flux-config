apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: pre-api
spec:
  interval: 10m
  releaseName: pre-api
  values:
    java:
      image: sdshmctspublic.azurecr.io/pre/api:prod-0f9ab5c-20240229081415 # {"$imagepolicy": "flux-system:pre-api"}
      disableTraefikTls: true
  chart:
    spec:
      chart: ./stable/pre-api
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 5m
  install:
    disableWait: true
  upgrade:
    disableWait: true
