apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: pre-api
spec:
  interval: 10m
  releaseName: pre-api
  values:
    java:
      image: sdshmctspublic.azurecr.io/pre/api:prod-9c90a77-20240115081826 # {"$imagepolicy": "flux-system:pre-api"}
      disableTraefikTls: true
  chart:
    spec:
      chart: ./stable/pre-api
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 5m
  install:
    disableWait: true
  upgrade:
    disableWait: true
