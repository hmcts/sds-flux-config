apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: stg-pre-api
spec:
  filterTags:
    pattern: "^staging-[a-f0-9]+-(?P<ts>[0-9]+)"
    extract: "$ts"
  policy:
    alphabetical:
      order: asc
  imageRepositoryRef:
    name: pre-api