apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pre-api-cron-vodafone-etl-migrate-exclusions
  namespace: pre
spec:
  releaseName: pre-api-cron-vodafone-etl-migrate-exclusions
  values:
    java:
      enabled: false
    job:
      enabled: true
      environment:
        TASK_NAME: MigrateExclusions
  chart:
    spec:
      chart: ./stable/pre-api
      sourceRef:
        kind: GitRepository
        name: hmcts-charts
        namespace: flux-system
      interval: 1m