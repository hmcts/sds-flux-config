apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pre-api-cron-process-capture-sessions
  namespace: pre
spec:
  values:
    global:
      jobKind: CronJob
    job:
      disableActiveClusterCheck: true
      schedule: "15 18 * * *" # 6.15pm every day
      image: hmctsprod.azurecr.io/pre/api:prod-0830d0e-20260311183157 # {"$imagepolicy": "flux-system:pre-api"}
