apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pre-api-cron-cleanup-live-events
  namespace: pre
spec:
  values:
    global:
      jobKind: CronJob
    job:
      schedule: "0 18 * * *" # 6pm every day (before CVP cron)
      image: sdshmctspublic.azurecr.io/pre/api:prod-74f120c-20260206130339 # {"$imagepolicy": "flux-system:pre-api"}
      environment:
        PORTAL_URL: https://portal.pre-recorded-evidence.justice.gov.uk/
