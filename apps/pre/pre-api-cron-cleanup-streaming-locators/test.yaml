apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pre-api-cron-cleanup-streaming-locators
  namespace: pre
spec:
  values:
    global:
      jobKind: CronJob
    job:
      schedule: "30 09 * * *" # 6:30 PM UTC
      image: hmctsprod.azurecr.io/pre/api:prod-c2bf036-20260715095846 # {"$imagepolicy": "flux-system:pre-api"}
      environment:
        PLATFORM_ENV_TAG: Testing
        PORTAL_URL: https://pre-portal.test.platform.hmcts.net/
