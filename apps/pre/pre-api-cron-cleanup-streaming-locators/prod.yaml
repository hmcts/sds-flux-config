apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pre-api-cron-cleanup-streaming-locators
  namespace: pre
spec:
  values:
    global:
      jobKind: CronJob
    job:
      schedule: "0 0 * * *" # Midnight every day
      image: sdshmctspublic.azurecr.io/pre/api:prod-7f9a950-20240826085920 # {"$imagepolicy": "flux-system:pre-api"}
