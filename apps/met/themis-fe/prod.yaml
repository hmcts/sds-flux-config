---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: example-app
  namespace: met
spec:
  values:
    nodejs:
      ingressHost: # ingress host e.g. 'jdbureau.dev.platform.hmcts.net'
      image: # image deployed to acr
      environment:
        # any prod specific environment variables
        
