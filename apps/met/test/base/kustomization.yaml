apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  - ../../../rbac/dev-role.yaml
namespace: met

patchesStrategicMerge:
  - ../../identity/test.yaml

keyVaults:
  libragob-test-kv:
    excludeEnvironmentSuffix: true