---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: jenkins
  namespace: jenkins
spec:
  values:
    controller:
      prometheus:
        enabled: true
        serviceMonitorAdditionalLabels:
          release: kube-prometheus-stack
      JCasC:
        configScripts:
          prometheus: |
            unclassified:
              prometheusConfiguration:
                path: "prometheus"
                defaultNamespace: "default"
                useAuthenticatedEndpoint: false
                collectingMetricsPeriodInSeconds: 120
                collectNodeStatus: true
                # Full per-build history remains in the CosmosDB ->
                # dtsse-dashboard-ingestion -> Postgres pipeline. Here we expose
                # the plugin's per-job "last build" metrics (bounded by number
                # of jobs x stages, not by build history) for runtime, per-stage
                # duration, pass/fail counts and disk usage. perBuildMetrics
                # stays disabled to avoid per-build-history cardinality blow-up.
                collectDiskUsage: true
                countSuccessfulBuilds: true
                countUnstableBuilds: true
                countFailedBuilds: true
                countNotBuiltBuilds: false
                countAbortedBuilds: true
                fetchTestResults: false
                perBuildMetrics: false
                appendParamLabel: false
                appendStatusLabel: false
                processingDisabledBuilds: false
