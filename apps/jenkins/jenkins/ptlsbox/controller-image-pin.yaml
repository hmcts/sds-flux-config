---
# Temporary pin to the cnp-jenkins-docker PR image for DTSPO-33801 (prometheus
# plugin validation). Must be applied after jenkins-controller-version.yaml.
# Remove once hmcts/cnp-jenkins-docker#1895 is merged and released.
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: jenkins
  namespace: jenkins
spec:
  values:
    controller:
      image:
        tag: pr-1895-2.564-1777
