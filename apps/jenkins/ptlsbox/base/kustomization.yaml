apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: jenkins
resources:
  - ../../base
  - ../../jenkins-webhook-relay/ptlsbox/jenkins-webhook-relay.enc.yaml

patchesStrategicMerge:
  - ../../jenkins-webhook-relay/ptlsbox/jenkins-webhook-relay.yaml
