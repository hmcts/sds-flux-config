apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base # This is apps/base instead of apps/hmi/base as preview doesn't install everything
  - ../../identity/hmi-azure-identity.yaml
namespace: hmi
patchesStrategicMerge:
  - ../../identity/dev.yaml