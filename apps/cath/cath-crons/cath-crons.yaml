apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cath-crons
spec:
  releaseName: cath-crons
  values:
    job:
      image: hmctspublic.azurecr.io/cath/cath-crons:prod-7b0d3ba-20260514133103 # {"$imagepolicy": "flux-system:cath-crons"}
      disableTraefikTls: true
  chart:
    spec:
      chart: cath-crons
      version: ">=0.0.1-0"
      sourceRef:
        kind: HelmRepository
        name: hmctsprod-oci # correct
        namespace: flux-system
      interval: 1m
  install:
    disableWait: true
  upgrade:
    disableWait: true
