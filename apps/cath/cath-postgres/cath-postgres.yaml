apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cath-postgres
spec:
  releaseName: cath-postgres
  values:
    nodejs:
      image: hmctspublic.azurecr.io/cath/cath-postgres:prod-b5e9528-20260618103716 # {"$imagepolicy": "flux-system:cath-postgres"}
      disableTraefikTls: true
  chart:
    spec:
      chart: cath-postgres
      version: ">=0.0.1-0"
      sourceRef:
        kind: HelmRepository
        name: hmctsprod-oci # correct
        namespace: flux-system
      interval: 1m
  install:
    disableWait: true
  upgrade:
    disableWait: true
