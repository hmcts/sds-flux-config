apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cath-postgres
spec:
  releaseName: cath-postgres
  values:
    nodejs:
      image: hmctspublic.azurecr.io/cath/cath-postgres:prod-13f998b-20260703112817 # {"$imagepolicy": "flux-system:cath-postgres"}
      disableTraefikTls: true
  chart:
    spec:
      chart: cath-postgres
      version: ">=0.0.1-0"
      sourceRef:
        kind: HelmRepository
        name: hmctsprod-oci # correct
        namespace: flux-system
      interval: 1m
  install:
    disableWait: true
  upgrade:
    disableWait: true
