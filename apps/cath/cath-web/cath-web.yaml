apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cath-web
spec:
  releaseName: cath-web
  values:
    nodejs:
      image: hmctspublic.azurecr.io/cath/cath-web:prod-735da3a-20260724093520 # {"$imagepolicy": "flux-system:cath-web"}
      disableTraefikTls: true
  chart:
    spec:
      chart: cath-web
      version: ">=0.0.1-0"
      sourceRef:
        kind: HelmRepository
        name: hmctsprod-oci # correct
        namespace: flux-system
      interval: 1m
  install:
    disableWait: true
  upgrade:
    disableWait: true
