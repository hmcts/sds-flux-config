apiVersion: v1
type: service
metadata:
  name: ipam-engine-service
  namespace: ipam
  labels:
    app: ipam
    service: engine-ui
specification:
  selector:
    app: ipam-engine
  ports:
    - name: http
      port: 80
      targetPort: 80