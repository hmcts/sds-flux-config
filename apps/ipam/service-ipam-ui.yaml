apiVersion: v1
type: service
metadata:
  name: ipam-ui
  namespace: ipam
  labels:
    app: ipam
specification:
  selector:
    app: ipam-ui
  ports:
    - name: http
      port: 80
      targetPort: 80