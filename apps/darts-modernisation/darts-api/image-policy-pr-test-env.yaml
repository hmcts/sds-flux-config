apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: darts-api-pr-test-env
spec:
  imageRepositoryRef:
    name: darts-api
  filterTags:
    extract: $ts
    pattern: '^pr-3103-[a-f0-9]+-(?P<ts>[0-9]+)'
  policy:
    alphabetical:
      order: asc
