apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: external-dns-private
  namespace: admin 
spec:
  releaseName: external-dns-private
  values:
    annotationFilter: external-dns-selector not in (traefik, traefik-no-proxy)
    logLevel: debug
    txtOwnerId: sds-demo-aks-active
