apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: traefik2-no-proxy
  namespace: admin
spec:
  values:
    service:
      spec:
        loadBalancerIP: "10.51.95.253"
    ingressClass:
      enabled: true
      isDefaultClass: false
    ports:
      web:
        redirectTo: websecure
