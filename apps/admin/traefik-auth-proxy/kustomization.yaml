apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../traefik2.yaml
  - oauth-auth.yaml
  - oauth-errors.yaml
  - oauth-headers.yaml
  - oauth-routes.yaml
patchesStrategicMerge:
  - traefik-auth-proxy.yaml